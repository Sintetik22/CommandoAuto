local ccccount = 0

local function killViciousBeeFunc()
for i,v in pairs(workspace.Monsters:GetChildren()) do
    if string.find(v.Name, "Stick Bug") then 
        pcall(function()
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v.CFrame
        end)
    end
end
end
for i,v in pairs(workspace.Monsters:GetChildren()) do
    if string.find(v.Name, "Stick Bug") then
            ccccount = ccccount + 1
            if ccccount == 5 then
                cccccount = 0
                Tween(0.5,v.Torso.CFrame - Vector3.new(17, 10, 0))
            else
            Tween(0.5,v.Head.CFrame + Vector3.new(17, 16, 0))
            end
        end
    end


